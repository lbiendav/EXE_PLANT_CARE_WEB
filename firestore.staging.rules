rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Staging web accesses Firestore only through the server Admin SDK (IAM).
    // Do not deploy this file to the shared mobile/local Firebase project.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
