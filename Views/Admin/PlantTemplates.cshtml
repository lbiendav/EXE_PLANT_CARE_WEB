@model List<HomePlant.Models.PlantTemplateModel>

@{
    ViewData["Title"] = "Quản lý mẫu cây";
}

<style>
    .admin-card img {
        height: 170px;
        width: 100%;
        object-fit: cover;
    }
</style>

<a asp-action="Dashboard" class="back-link">
    ← Quản trị
</a>

<div class="mb-4">
    <span class="kicker">Quản trị</span>
    <h1 class="mb-0">Mẫu cây cũ</h1>
    <p class="text-muted mt-2 mb-0">Chỉ giữ để tương thích với cây người dùng đã tạo trước đây. Hãy quản lý dữ liệu mới trong “Danh mục loài cây”.</p>
</div>

<div class="row g-4">
    @foreach (var t in Model)
    {
        <div class="col-lg-3 col-md-4 col-sm-6">
            <div class="card admin-card h-100">
                <img src="@(string.IsNullOrEmpty(t.ImageUrl) ? "https://i.ibb.co/MkMj38WM/plant.png" : t.ImageUrl)" alt="@t.Name" />

                <div class="card-body d-flex flex-column">
                    <div class="d-flex justify-content-between align-items-start">
                        <h6 class="fw-bold mb-0">@t.Name</h6>
                        @if (t.IsFeatured)
                        {
                            <span class="badge bg-warning text-dark">Nổi bật</span>
                        }
                    </div>

                    <small class="text-muted fst-italic mb-2">@t.ScientificName</small>
                    <p class="small text-muted mb-2" style="min-height:60px;">@t.Description</p>

                    <div class="mt-auto text-muted small">Dữ liệu tương thích · chỉ đọc</div>
                </div>
            </div>
        </div>
    }
</div>
